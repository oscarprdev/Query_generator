type TablesViewProps = {
	projectTitle: string;
};

const TablesView = ({ projectTitle }: TablesViewProps) => {
	return <section className="h-full w-full border border-red-500"></section>;
};

export default TablesView;
